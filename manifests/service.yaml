apiVersion: v1
kind: Service
metadata:
  name: ai-agent-service
spec:
  selector:
    app: ai-agent
  ports:
    - port: 80 # Der Service-Port (extern)
      targetPort: 8080 # Der Container-Port (intern)
  type: ClusterIP # Standardmäßig nur interner Cluster-Zugriff